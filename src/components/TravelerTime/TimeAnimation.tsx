import React from 'react';

interface TimeAnimationProps {
    currentTime: number;
}

const TimeAnimation = (props: TimeAnimationProps) => {
    const { currentTime } = props;

};

export default TimeAnimation;